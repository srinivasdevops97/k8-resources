# PersistentVolumeClaim(PVC) creation
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: efs-dynamic
spec:
  storageClassName: "efs-expense"
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 3Gi
---
# Pod creation
apiVersion: v1
kind: Pod
metadata:
  name: efs-dynamic
  labels:
    purpose: efs-dynamic
spec:
  # nodeSelector:
  #   topology.kubernetes.io/zone: us-east-1b
  containers:
  - name: nginx
    image: nginx
    volumeMounts:   # equivalent to docker run -v <host-path>:<container-path>
    - name: efs-dynamic
      mountPath: /usr/share/nginx/html
  volumes:
  - name: efs-dynamic
    persistentVolumeClaim:
      claimName: efs-dynamic
---
# Load Balancer creation
kind: Service
apiVersion: v1
metadata:
  name: nginx-lb  # name of the service
spec:
  type: LoadBalancer
  selector:
    purpose: efs-dynamic
  ports:       # list of the ports
  - name: nginx-svc-port
    protocol: TCP
    port: 80      # service port
    targetPort: 80     # container port(another port)
    nodePort: 30007