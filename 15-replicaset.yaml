apiVersion: apps/v1
kind: ReplicaSet
metadata:
  name: expense
  labels:
    app: nginx
    tier: frontend
spec:
  # modify replicas according to your case
  replicas: 3
  selector:
    matchLabels:
      app: nginx
      tier: frontend
  # this is the pod definition
  template:
    metadata:
      # thses labels belongs to pod
      labels:
        app: nginx
        tier: frontend
    spec:
      containers:
      - name: nginx
        image: nginx